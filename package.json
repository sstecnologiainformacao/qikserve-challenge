{
  "name": "checkout-supermarket-challenger",
  "version": "0.1.0",
  "private": true,
  "engines": {
    "node": ">=12.0.0"
  },
  "scripts": {
    "server": "yarn --cwd checkout-supermarket-server start",
    "client": "yarn --cwd checkout-supermarket start",
    "start": "concurrently \"yarn server\" \"yarn client\""
  },
  "dependencies": {
    "concurrently": "^5.3.0"
  }
}
